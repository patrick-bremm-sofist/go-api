Criar um dotenv e definir a variavel MONGOURI usando o link de conexão como no exemplo do docker-compose

Comando para rodar local:
`go run main.go`

Comando para rodar local e atualizando o código em tempo real no docker:
`docker-compose up -d`